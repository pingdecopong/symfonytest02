{% extends '::base.html.twig' %}

{% block body -%}
    <h1>TBDepartment creation</h1>

    <form action="{{ path('tbdepartment_create') }}" novalidate method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <p>
            <button type="submit">Create</button>
        </p>
    </form>

        <ul class="record_actions">
    <li>
        <a href="{{ path('tbdepartment') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
