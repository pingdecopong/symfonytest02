{% extends '::base.html.twig' %}
{% form_theme form 'pingdecopongFormFreezeBundle:Form:form_layout.html.twig' %}

{% block body -%}
    <h1>SystemUser edit</h1>

    <form action="{{ path('systemuser_edit', { 'id': entity.id }) }}" method="post" {{ form_enctype(form) }} name="systemuser_edit_form">
        {{ form_widget(form) }}
        <p>
            {% if mode == 'input' %}
                <button onclick="postConfirm()">確認</button>
            {% elseif mode == 'confirm' %}
                <button onclick="postBack()">戻る</button>
                <button onclick="postSubmit()">登録</button>
            {% endif %}
        </p>
        <input id="_button_action" type="hidden" value="" name="_button_action">
    </form>

        <ul class="record_actions">
    <li>
        <a href="{{ path('systemuser') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('systemuser_delete', { 'id': entity.id }) }}">
            Delete
        </a>
    </li>
</ul>
{% endblock %}

{% block javascripts %}
    <script type="text/javascript">
        //<![CDATA[
        function postConfirm(){
            document.systemuser_edit_form._button_action.value = "confirm";
            document.systemuser_edit_form.submit();
        }
        function postSubmit(){
            document.systemuser_edit_form._button_action.value = "submit";
            document.systemuser_edit_form.submit();
        }
        function postBack(){
            document.systemuser_edit_form._button_action.value = "";
            document.systemuser_edit_form.submit();
        }
        //]]>
    </script>
{% endblock %}
